# === API (instalación limpia) ===
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# === Audio / DSP (versiones modernas, compatibles con Python 3.12) ===
numpy==1.26.4          # Basic Pitch pide numpy antiguo; forzamos 1.26.4 (sí soporta 3.12)
scipy>=1.11
librosa>=0.10
soundfile>=0.12
resampy>=0.4
mir_eval>=0.7

# === MIDI / notación ===
pretty_midi>=0.2.10
mido>=1.3
music21>=9.1

# === Transcripción (backend ONNX, NO TensorFlow) ===
onnxruntime>=1.17
# ⚠️ basic-pitch se instala aparte con --no-deps para no arrastrar TensorFlow ni
#    numpy antiguo. Lo hace el script scripts/setup.ps1. Manualmente:
#        pip install --no-deps basic-pitch
# basic-pitch==0.4.0

# === Desarrollo / testing ===
pytest>=8.0
httpx>=0.27
ruff>=0.4

# --- Separación de voz con Demucs (opcional, pesado ~2.5 GB) ---
# torch/torchaudio con CUDA se instalan desde el índice de PyTorch:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install demucs
# (Para CPU en vez de GPU, usa --index-url .../whl/cpu)
# demucs>=4.0
